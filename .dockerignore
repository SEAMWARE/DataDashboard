# Node dependencies (already installed inside the container)
node_modules
backend/node_modules
config/application.yaml

# public directory
public/config

# Angular build output (except during multi-stage builds)
dist

# Editor & OS-specific files
.vscode
.idea

# Git-related files
.git
